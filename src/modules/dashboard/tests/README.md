Dashboard module tests should be added here. Use existing test patterns.

Examples:
- tests/dashboard.controller.test.js
- tests/dashboard.service.test.js

Run `npm test` (test runner not configured in this repo yet).
